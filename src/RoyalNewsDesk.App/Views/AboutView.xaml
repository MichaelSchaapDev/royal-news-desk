<UserControl
  x:Class="RoyalNewsDesk.App.Views.AboutView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:res="clr-namespace:RoyalNewsDesk.App.Resources"
  TextElement.Foreground="{DynamicResource TextFillColorPrimaryBrush}">
  <StackPanel MaxWidth="720" Margin="28,20,28,28" HorizontalAlignment="Stretch">
    <TextBlock
      Margin="0,0,0,10"
      FontSize="26"
      FontWeight="SemiBold"
      Text="{x:Static res:Strings.About_Header}" />
    <TextBlock Margin="0,0,0,16" Text="{x:Static res:Strings.About_MadeFor}" TextWrapping="Wrap" />
    <StackPanel Orientation="Horizontal">
      <TextBlock FontWeight="SemiBold" Text="{x:Static res:Strings.About_Version}" />
      <TextBlock Margin="8,0,0,0" Text="{Binding VersionText}" />
    </StackPanel>
    <TextBlock Margin="0,10,0,0">
      <Hyperlink NavigateUri="{Binding RepositoryUrl}" RequestNavigate="OnRequestNavigate">
        <Run Text="{Binding RepositoryUrl, Mode=OneWay}" />
      </Hyperlink>
    </TextBlock>
  </StackPanel>
</UserControl>
