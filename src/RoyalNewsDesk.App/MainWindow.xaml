<ui:FluentWindow
  x:Class="RoyalNewsDesk.App.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
  xmlns:res="clr-namespace:RoyalNewsDesk.App.Resources"
  xmlns:vm="clr-namespace:RoyalNewsDesk.App.ViewModels"
  Title="{x:Static res:Strings.App_Title}"
  Width="1240"
  Height="800"
  MinWidth="960"
  MinHeight="640"
  Background="{DynamicResource ApplicationBackgroundBrush}"
  ExtendsContentIntoTitleBar="True"
  WindowBackdropType="None"
  WindowCornerPreference="Round"
  WindowStartupLocation="CenterScreen">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <ui:TitleBar Grid.Row="0" Title="{x:Static res:Strings.App_Title}" />

    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="230" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <Border
        Grid.Column="0"
        Padding="10,14"
        Background="{DynamicResource ControlFillColorDefaultBrush}">
        <DockPanel>
          <TextBlock
            DockPanel.Dock="Top"
            Margin="12,0,0,18"
            FontSize="15"
            FontWeight="SemiBold"
            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
            Text="{x:Static res:Strings.App_Title}"
            TextWrapping="Wrap" />

          <ListBox
            BorderThickness="0"
            Background="Transparent"
            ItemsSource="{Binding NavItems}"
            SelectedItem="{Binding SelectedNavItem}">
            <ListBox.ItemTemplate>
              <DataTemplate DataType="{x:Type vm:NavItem}">
                <StackPanel Margin="4,6" Orientation="Horizontal">
                  <ui:SymbolIcon FontSize="18" Symbol="{Binding Icon}" />
                  <TextBlock Margin="12,0,0,0" FontSize="14" Text="{Binding Label}" />
                </StackPanel>
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
        </DockPanel>
      </Border>

      <ContentControl
        Grid.Column="1"
        Content="{Binding CurrentPage}" />
    </Grid>
  </Grid>
</ui:FluentWindow>
