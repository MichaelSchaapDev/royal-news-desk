# Pinned SadTalker runtime set for the presenter bundles.
# torch/torchvision install separately per variant (cpu vs cu121 index).
# basicsr and gfpgan install separately with --no-deps (their tb-nightly and
# gradio requirements are dead weight; real runtime deps are listed here).
numpy==1.23.4
# face_alignment installs separately with --no-build-isolation: its setup.py
# imports the package (and so filterpy) while building metadata.
imageio==2.19.3
imageio-ffmpeg==0.4.7
librosa==0.9.2
numba==0.56.4
llvmlite==0.39.1
resampy==0.3.1
pydub==0.25.1
scipy==1.10.1
kornia==0.6.8
tqdm==4.66.4
yacs==0.1.8
pyyaml==6.0.1
joblib==1.1.0
scikit-image==0.19.3
facexlib==0.3.0
safetensors==0.4.3
# av deliberately omitted: SadTalker only imports it in training code
# (src/face3d/models/arcface_torch), never on the inference path, and its
# Windows wheels are unreliable.
# filterpy installs separately with --no-build-isolation: its setup.py
# imports the package while building metadata.
addict==2.4.0
future==1.0.0
lmdb==1.4.1
yapf==0.40.2
requests==2.31.0
opencv-python==4.9.0.80
